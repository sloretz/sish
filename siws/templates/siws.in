[siws]
version = ${siws_version}
workspace_path = ${workspace_path}

[containers]
# Paths to the containers available to this workspace
${container_paths}

# The sandbox was created using the commands
${comment_build_commands}

[ref]
_shell = singularity shell --no-home --no-mount cwd ${binds} --pwd "$${siws:path}"

[commands]
# CLI tool commands and the programs they execute when run to access containers
${container_commands}
